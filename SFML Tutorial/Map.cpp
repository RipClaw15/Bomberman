#include "Map.h"
#include <iostream>

using namespace std;

Map::Map(Texture* texture, int w, int h) 
   
{
    this->start = start;
    Clear();
    Generate(texture, 4, 4);
}

Map::~Map()
{
}

void Map::Clear()
{
	
}

void Map::Generate(Texture* texture, int x, int y)
{
    int i, j;
	
    for (i = 0; i <= x; i++);
    {cout << x;
        for (j = 0; j <= y; j++);
        {cout << y;
            
        }
    }



}
